import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sistemaai.android',
  appName: 'sistema-ai-android',
  webDir: 'www'
};

export default config;
